{
  "name": "arch-analyzer",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "lerna run test",
    "test:coverage": "lerna run test:coverage",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^8.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
